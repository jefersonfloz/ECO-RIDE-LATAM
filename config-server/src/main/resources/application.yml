spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/faritteranacosta/ECO-RIDE-LATAM
          default-label: main
          search-paths:
            - config-server

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
